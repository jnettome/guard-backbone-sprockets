guard-backbone-sprockets
========================

A Guardfile example to save our lives when working on Mac with a Windows VM

To run:

> bundle exec guard start -w ../path-to-project

_VMWare Network - Bridge_